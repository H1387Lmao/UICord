[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "uicord"
version = "5.0.4"
description = "Components V2 helper for Discord bots"
requires-python = ">=3.10"
dependencies = ["py-cord>=2.7", "ply", "pygments"]
readme = "README.md"

[project.urls]
Homepage = "https://github.com/H1387Lmao/uicord"
Repository = "https://github.com/H1387Lmao/uicord"
BugTracker = "https://github.com/H1387Lmao/uicord/issues"

[tool.setuptools.packages.find]
where = ["."]

[tool.uv.workspace]
members = [
    ".",
]

[tool.uv.sources]
uicord = { workspace = true }

[dependency-groups]
dev = [
    "uicord",
]

