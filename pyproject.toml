[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "UICord"
version = "2.0.0"
description = "Components V2 helper for Discord bots"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["uicord"]
